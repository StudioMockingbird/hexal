{
  "id": "values-and-bindings",
  "name": "Values and Bindings",
  "snippets": [
    {
      "id": "values-scalar-dashboard",
      "name": "Scalar dashboard",
      "description": "Declares representative signed, unsigned, floating-point, boolean, and size values, then combines them in a small calculation.",
      "entrypoint": "app.hex",
      "sources": {"app.hex": [
        "let fan_speed: Int16= -5",
        "let power_level: UInt8= 42",
        "let balance: Float32= 0.75",
        "let enabled: Bool= true",
        "let steps: Size= 4",
        "let mut adjusted: Float64= balance.to\u003cFloat64\u003e() + power_level.to\u003cFloat64\u003e()",
        "if enabled then",
        "    adjusted = adjusted + steps.to<Float64>()",
        "end",
        "adjusted = adjusted - (fan_speed.to<Float64>() * 0.25)"
      ]},
      "features": ["bindings", "scalar-types"],
      "reservedWords": ["mut", "if", "end", "true", "then"]
    },
    {
      "id": "values-mutable-accumulator",
      "name": "Mutable accumulator",
      "description": "Updates a mutable integer through several meaningful steps and returns the total.",
      "entrypoint": "app.hex",
      "sources": {"app.hex": [
        "fun total_after_steps(seed: Int32): Int32 do",
        "    let mut total: Int32= seed",
        "    total = total + 5",
        "    total = total * 2",
        "    total = total - 3",
        "    return total",
        "end",
        "let result: Int32= total_after_steps(10)"
      ]},
      "features": ["bindings", "functions"],
      "reservedWords": ["fun", "mut", "return", "end", "do"]
    },
    {
      "id": "values-packet-header",
      "name": "Packet header",
      "description": "Decodes flags using hexadecimal, binary, octal, and separated integer literals.",
      "entrypoint": "app.hex",
      "sources": {"app.hex": [
        "let version: UInt8= 0x04",
        "let flags: UInt8= 0b1010_0101",
        "let mode: UInt8= 0o17",
        "let port: UInt16= 0x1F_90",
        "let low: UInt8= flags \u0026 0x0F",
        "let high: UInt8= flags \u003e\u003e 4",
        "let total: UInt32= version.to\u003cUInt32\u003e() + low.to\u003cUInt32\u003e() + high.to\u003cUInt32\u003e() + mode.to\u003cUInt32\u003e() + port.to\u003cUInt32\u003e()"
      ]},
      "features": ["literals", "bitwise"]
    },
    {
      "id": "values-text-record",
      "name": "Text record",
      "description": "Combines String, Strand, Byte, and Rune values in one small record-like object.",
      "entrypoint": "app.hex",
      "sources": {"app.hex": [
        "type Entry is struct name: Strand, note: String, first: Rune, kind: Byte end",
        "let entry: Entry= Entry(name = \"hexal\", note = \"hi\", first = 'h', kind = b'\\x01')",
        "let initial: Rune= entry.first",
        "let header: Size= entry.name.length()",
        "let kind_value: Byte= entry.kind"
      ]},
      "features": ["objects", "text", "literals"],
      "reservedWords": ["type", "struct", "end"]
    },
    {
      "id": "values-empty-struct",
      "name": "Empty struct",
      "description": "A nominal marker type with no members; every value of it compares equal.",
      "entrypoint": "app.hex",
      "sources": {"app.hex": [
        "type Marker is struct end",
        "let first: Marker= Marker()",
        "let second: Marker= Marker()",
        "let same: Bool= first == second"
      ]},
      "features": ["objects"],
      "reservedWords": ["type", "struct", "end"]
    },
    {
      "id": "values-immutable-snapshot",
      "name": "Immutable snapshot",
      "description": "Copies an object, mutates the original, and reads the unchanged copy to demonstrate value copying.",
      "entrypoint": "app.hex",
      "sources": {"app.hex": [
        "type Meter is struct mut reading: Int32, unit: Strand end",
        "let mut live: Meter= Meter(reading = 10, unit = \"kwh\")",
        "let snapshot: Meter= live",
        "live.reading = 42",
        "let saved: Int32= snapshot.reading"
      ]},
      "features": ["objects", "copying"],
      "reservedWords": ["type", "mut"]
    },
    {
      "id": "values-score-alias",
      "name": "Score alias",
      "description": "Defines a domain-specific numeric alias and uses it in an object.",
      "entrypoint": "app.hex",
      "sources": {"app.hex": [
        "type Score is Int32",
        "type Match is struct mut home: Score, mut away: Score end",
        "let mut game: Match= Match(home = 2, away = 1)",
        "game.home = game.home + 1"
      ]},
      "features": ["aliases", "objects"],
      "reservedWords": ["type", "mut"]
    },
    {
      "id": "values-boolean-policy",
      "name": "Boolean policy",
      "description": "Combines comparisons with and, or, and boolean negation to choose a policy.",
      "entrypoint": "app.hex",
      "sources": {"app.hex": [
        "let age: Int32= 21",
        "let has_ticket: Bool= true",
        "let vip: Bool= false",
        "let mut admitted: Bool= ((age \u003e= 18) and has_ticket) or vip",
        "let mut rejected: Bool= !admitted",
        "rejected = rejected and (age < 16)"
      ]},
      "features": ["bindings", "truthiness"],
      "reservedWords": ["and", "or", "true", "false"]
    },
    {
      "id": "values-layout-total",
      "name": "Compile-time layout total",
      "description": "Combines size_of and align_of for an object type into one total.",
      "entrypoint": "app.hex",
      "sources": {"app.hex": [
        "type Entry is struct tag: UInt8, value: Float64 end",
        "let entry: Entry= Entry(tag = 1, value = 0.5)",
        "let size: Size= size_of\u003cEntry\u003e()",
        "let alignment: Size= align_of\u003cEntry\u003e()",
        "let total: Size= size + alignment"
      ]},
      "features": ["objects", "layout"],
      "reservedWords": ["type"]
    },
    {
      "id": "values-commented-config",
      "name": "Commented configuration",
      "description": "Groups related settings with line and block comments while using the values.",
      "entrypoint": "app.hex",
      "sources": {"app.hex": [
        "--- Render settings",
        "--[ These values drive the default render. ]--",
        "let mut width: Int32= 800",
        "let mut height: Int32= 600",
        "--- Two halves of the canvas",
        "let left: Int32= width / 2",
        "let right: Int32= height / 2"
      ]},
      "features": ["comments", "bindings"],
      "reservedWords": ["mut"]
    },
    {
      "id": "values-literal-normalization",
      "name": "Literal normalization",
      "description": "Uses escaped strings, bytes, runes, and numeric separators together in a normalized value.",
      "entrypoint": "app.hex",
      "sources": {"app.hex": [
        "let greeting: String= \"a\\\"b\\\\c\\nd\\te\"",
        "let marker: Byte= b'\\x41'",
        "let symbol: Rune= '\\u{03BB}'",
        "let mask: UInt32= 0xFF00_FF00",
        "let combined: UInt32= mask + symbol.to\u003cUInt32\u003e()"
      ]},
      "features": ["literals", "text"]
    }
  ]
}
