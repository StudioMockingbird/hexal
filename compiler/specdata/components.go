package specdata

// Runtime-component records arrive here: one record per component, naming the
// files it owns, the runtime dependencies it selects, and the C headers it
// requires. The generator's per-component demand logic stays in Go.
